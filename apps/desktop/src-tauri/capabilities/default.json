{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base permissions of the main window (deny-by-default, minimal set)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
